# ADR-006：一个数据库文件代表一个工作空间

- 状态：Accepted
- 日期：2026-09-04

## 决策

删除统一 `owner_id` 字段。一个 SQLite 文件就是一个单用户工作空间；代码和查询不保留伪多租户逻辑。

## 理由

没有隔离校验的恒定 `owner_id = 1` 不能真正降低多用户迁移成本，反而污染所有 schema 和查询。未来多用户需要完整身份、授权、索引和数据迁移设计。

## 后果

若未来进入多工作空间阶段，将通过新 ADR 引入 `workspace_id` 和完整隔离模型，而不是沿用本 MVP 的假钩子。

