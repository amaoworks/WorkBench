# ADR-001：Go 模块化单体

- 状态：Accepted
- 日期：2026-09-04

## 决策

整体后端使用 Go，以单进程模块化单体部署。业务模块作为 Go package 在编译期进入可执行文件；运行时只支持启停已编译模块，不支持动态加载二进制插件。

## 理由

该方案符合单文件、本地优先和低运维目标，也保留清晰的模块边界。动态 Go plugin 的平台限制、版本耦合和发布复杂度不适合 MVP。

## 后果

新增模块需要重新构建；模块启停必须通过统一 gate 实现。Rust 仅可作为未来经性能测量证明必要的独立子进程，不改变整体架构。

